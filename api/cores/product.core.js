const productModel = require("../models/product.model")

const createProduct = (req, res) => {
    try {
    const {title, dec, img, category, }  = req.body;
    const newProduct = productModel.create({

    })
        const 
    } catch (error) {
        
    }
}